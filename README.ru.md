# База знаний OpenClaw

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Node.js Version](https://img.shields.io/badge/node-%3E%3D18-brightgreen)](https://nodejs.org/)
[![OpenClaw](https://img.shields.io/badge/OpenClaw-2026.2.12-blue)](https://github.com/openclaw/openclaw)

**[🇬🇧 English version](README.md)** | **[🤖 Для AI-ассистентов](AGENTS.md)**

---

🤖 **Самообновляемая векторная база знаний для OpenClaw** — семантический поиск по документации, коду и конфигурациям с автоматическим отслеживанием релизов.

Идеально для:
- **AI-агентов**: Claude Code, Cursor или кастомные агенты, которым нужен контекст OpenClaw
- **Разработчиков**: Быстрый семантический поиск паттернов API и опций конфигурации
- **DevOps**: Самообновляемая документация, которая всегда актуальна
- **Поддержки**: Точные ответы из реального кода, а не устаревшей документации

## Что это делает

- **Отслеживает релизы**: Мониторит GitHub OpenClaw на предмет новых версий
- **Самообновляется**: Автоматически скачивает новые релизы и переиндексирует
- **Векторный поиск**: Семантический поиск по документации, коду, навыкам, схемам конфигурации
- **Расширение запросов**: Понимает аббревиатуры (tts→text-to-speech, tg→telegram)
- **Гибридный рейтинг**: Объединяет векторное сходство + сопоставление ключевых слов (RRF fusion)

## Требования

- **Node.js** 18+ (с npm/pnpm)
- **Git** для отслеживания upstream кода
- **OpenAI API ключ** для эмбеддингов (text-embedding-3-small)
- **Linux с systemd** (для auto-update таймера) или macOS (для LaunchAgent)

## Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone https://github.com/mrkhachaturov/openclaw-kb.git
cd openclaw-kb

# 2. Установить зависимости
npm install

# 3. Настроить
cp .env.example .env
# Отредактировать .env и добавить OPENAI_API_KEY

# 4. Клонировать upstream OpenClaw код
git clone https://github.com/openclaw/openclaw.git ../source

# 5. Запустить начальную индексацию
node index.js

# 6. Установить auto-update таймер (опционально)
./install.sh
```

## Структура директорий

```
openclaw-kb/
├── README.md              # Этот файл
├── install.sh             # Установщик auto-update таймера
├── sync-latest-tag.sh     # Скрипт auto-update (отслеживает релизы)
├── index.js               # Движок индексации
├── query.js               # Интерфейс запросов
├── lib/                   # Основные модули
│   ├── chunker.js         # Разбиение текста на чанки
│   ├── config.js          # Конфигурация KB
│   ├── embedder.js        # OpenAI эмбеддинги
│   └── search.js          # Гибридный поиск (vector + BM25)
├── .env                   # API ключи (создать из .env.example)
└── data/                  # База SQLite (создается автоматически)
    └── kb.db              # Векторный индекс + чанки

Соседняя директория (обязательно):
../source/                 # Клон OpenClaw git (отслеживается sync скриптом)
```

## Использование

### Ручные запросы

```bash
# Общий поиск (всё содержимое)
node query.js "Как настроить Telegram?"

# Только документация
node query.js "режимы sandbox" --docs

# Только код (детали реализации)
node query.js "выполнение инструментов" --code

# Только навыки
node query.js "вызов внешнего API" --skills

# Показать статистику
node query.js --stats
```

### Программный доступ (Node.js)

```javascript
import { query } from './lib/search.js'

const results = await query('Как настроить вебхуки?', {
  topK: 5,
  sources: ['docs', 'config']
})

for (const result of results) {
  console.log(`${result.path}:${result.lines}`)
  console.log(result.snippet)
  console.log(`Оценка: ${result.score}\n`)
}
```

### Интеграция с justfile

Добавьте в `justfile` вашего проекта:

```makefile
# Запросить базу знаний OpenClaw
kb-query query *args:
    #!/usr/bin/env bash
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    node /path/to/openclaw-kb/query.js "{{ query }}" {{ args }}

kb-docs query *args:
    node /path/to/openclaw-kb/query.js "{{ query }}" --docs {{ args }}

kb-code query *args:
    node /path/to/openclaw-kb/query.js "{{ query }}" --code {{ args }}
```

## Настройка Auto-Update

Установочный скрипт создает автоматическое отслеживание upstream релизов:

```bash
./install.sh
```

**Что он делает:**
- Создает systemd таймер (Linux) или LaunchAgent (macOS)
- Запускается каждые 2 часа
- Проверяет новые релизы OpenClaw
- Автоматически скачивает и переиндексирует, если изменились файлы, относящиеся к KB

**Ручная синхронизация:**
```bash
./sync-latest-tag.sh
```

## Интеграция с AI-ассистентами

**Для Claude Code, Cursor, Zed и других AI инструментов:**

См. [AGENTS.md](AGENTS.md) для полного руководства по интеграции с:
- Инструкциями по настройке для разных AI платформ
- Режимами запросов и лучшими практиками
- Примерами конфигураций агентов
- Использованием программного API

**Быстрый пример для Claude Code:**
```bash
# Создать конфигурацию агента
cp docs/upstream-knowledge.md ~/.claude/agents/

# Использовать в Claude Code
@upstream-knowledge "Как настроить sandbox?"
```

См. [docs/upstream-knowledge.md](docs/upstream-knowledge.md) для примера Claude Code агента.

## Интеграция с justfile (опционально)

Если вы используете [just](https://github.com/casey/just) для запуска задач, добавьте эти рецепты в `justfile` вашего проекта:

```makefile
# Путь к установке openclaw-kb
KB_DIR := "/path/to/openclaw-kb"

# Запросить базу знаний OpenClaw (всё содержимое)
kb-query query *args:
    #!/usr/bin/env bash
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    node {{ KB_DIR }}/query.js "{{ query }}" {{ args }}

# Запросить только документацию
kb-docs query *args:
    #!/usr/bin/env bash
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    node {{ KB_DIR }}/query.js "{{ query }}" --docs {{ args }}

# Запросить только код реализации
kb-code query *args:
    #!/usr/bin/env bash
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    node {{ KB_DIR }}/query.js "{{ query }}" --code {{ args }}

# Запросить только навыки
kb-skills query *args:
    #!/usr/bin/env bash
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    node {{ KB_DIR }}/query.js "{{ query }}" --skills {{ args }}

# Режим верификации (документация + перекрёстная проверка с кодом)
kb-verify query *args:
    #!/usr/bin/env bash
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    node {{ KB_DIR }}/query.js "{{ query }}" --verify {{ args }}

# Показать статистику KB
kb-stats:
    #!/usr/bin/env bash
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    node {{ KB_DIR }}/query.js --stats

# Переиндексировать базу знаний
reindex:
    #!/usr/bin/env bash
    export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    node {{ KB_DIR }}/index.js

# Ручная синхронизация с последним релизом
kb-auto-update:
    {{ KB_DIR }}/sync-latest-tag.sh
```

Затем используйте в терминале:
```bash
just kb-query "Как настроить Telegram?"
just kb-docs "режимы sandbox"
just kb-code "выполнение инструментов"
just kb-stats
```

## Как это работает

### Конвейер индексации

1. **Обнаружение источников**: Сканирует upstream/source на документацию, код, навыки, конфиг
2. **Разбиение на чанки**: Разделяет файлы на семантические чанки (500-2000 токенов)
3. **Эмбеддинг**: Генерирует OpenAI эмбеддинги (text-embedding-3-small)
4. **Хранение**: SQLite с расширением sqlite-vec для векторного сходства

### Конвейер запросов

1. **Расширение запроса**: Расширяет аббревиатуры и синонимы
2. **Векторный поиск**: Находит семантически похожие чанки
3. **Поиск по ключевым словам**: Рейтинг BM25 для точного совпадения терминов
4. **RRF fusion**: Объединяет рейтинги (чанки с обоими сигналами ранжируются выше)
5. **Дедупликация**: Объединяет перекрывающиеся чанки

### Процесс Auto-Update

```
Таймер срабатывает (каждые 2 часа)
  ↓
Получить upstream теги
  ↓
Последний тег (например, v2026.2.12)
  ↓
Сравнить с текущим checkout
  ↓
Если отличается:
  ├─ Checkout нового тега
  ├─ Проверить какие файлы изменились
  └─ Если релевантны для KB → переиндексировать
```

## Конфигурация

Отредактируйте `.env`:

```bash
# OpenAI API (обязательно)
OPENAI_API_KEY=sk-...

# Модель эмбеддингов (опционально, по умолчанию: text-embedding-3-small)
EMBEDDING_MODEL=text-embedding-3-small

# Размер чанка (опционально, по умолчанию: 1000)
CHUNK_SIZE=1000

# Перекрытие (опционально, по умолчанию: 200)
CHUNK_OVERLAP=200
```

## Разработка

```bash
# Запустить индексацию с отладочным выводом
DEBUG=kb:* node index.js

# Тестовый запрос без индексации
node query.js "тестовый запрос" --json

# Сравнить разные стратегии поиска
node compare-results.js "тестовый запрос"

# Проверить качество чанков
node check-chunks.js
```

## Производительность

- **Время индексации**: ~10-15 минут для полного кодовой базы OpenClaw (~7700 чанков из 2500 файлов)
- **Время запроса**: <100мс (гибридный поиск)
- **Хранилище**: ~80МБ база SQLite
- **Стоимость**: ~$0.15 за полную переиндексацию (OpenAI эмбеддинги)

## Устранение неполадок

**"Результаты не найдены"**
- Проверьте что upstream код клонирован: `ls ../source`
- Убедитесь что индексация завершена: `node query.js --stats`

**"Ошибка OpenAI API"**
- Убедитесь что `.env` содержит валидный `OPENAI_API_KEY`
- Проверьте квоту API: https://platform.openai.com/usage

**"sqlite-vec не найден"**
- Требуется Node.js 18+ с нативной поддержкой sqlite
- Установите зависимости: `npm install`

**Auto-update не работает**
- Проверьте статус таймера: `systemctl status openclaw-kb-sync.timer`
- Просмотрите логи: `journalctl -u openclaw-kb-sync -f`
- Протестируйте вручную: `./sync-latest-tag.sh`

## Лицензия

MIT License — см. [LICENSE](LICENSE)

## Внесение вклада

Это отдельный инструмент для отслеживания релизов OpenClaw. Для самого OpenClaw см.: https://github.com/openclaw/openclaw
